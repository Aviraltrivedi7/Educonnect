import { Badge } from "@/components/ui/badge";
import { Button } from "@/components/ui/button";
import { Card, CardContent, CardDescription, CardHeader, CardTitle } from "@/components/ui/card";
import { Switch } from "@/components/ui/switch";
import { trpc } from "@/lib/trpc";
import { Archive, ArchiveRestore, Download, FileSpreadsheet, History, Loader2 } from "lucide-react";
import { useState } from "react";
import { toast } from "sonner";

export default function AdminComparisonSharingExportHistory() {
  const utils = trpc.useUtils();
  const [archived, setArchived] = useState(false);
  const exports = trpc.workspace.adminComparisonSharingAuditExports.useQuery({ archived });
  const setArchive = trpc.workspace.setAdminComparisonSharingAuditExportArchived.useMutation({ onSuccess: (_, variables) => { utils.workspace.adminComparisonSharingAuditExports.invalidate(); toast.success(variables.archived ? "Export archived from the active history." : "Export restored to the active history."); }, onError: error => toast.error(error.message) });
  return <Card className="border-[#dce7f5] bg-[linear-gradient(135deg,#f8fbff_0%,#ffffff_76%)]"><CardHeader className="border-b border-[#e6edf7]"><div className="flex items-start justify-between gap-3"><div><div className="mb-2 inline-flex items-center gap-2 rounded-full bg-[#eaf0fc] px-2.5 py-1 text-[10px] font-bold uppercase tracking-[.12em] text-[#506f9d]"><History className="size-3" />Managed report history</div><CardTitle className="text-lg">Sharing-audit downloads</CardTitle><CardDescription>Review and retrieve completed comparison-sharing CSV reports generated by administrators in this school.</CardDescription></div><span className="grid size-10 place-items-center rounded-2xl bg-[#e8f0fc] text-[#5376a6]"><FileSpreadsheet className="size-5" /></span></div></CardHeader><CardContent className="space-y-3 pt-4"><div className="flex items-center justify-between rounded-xl border border-[#dbe6f5] bg-white/80 px-3 py-2.5"><div><strong className="text-xs text-[#496784]">Show archived reports</strong><span className="mt-0.5 block text-[11px] text-[#7b8fa5]">Archived files stay available and can be restored.</span></div><Switch checked={archived} onCheckedChange={setArchived} aria-label="Show archived sharing audit exports" /></div>{exports.isLoading ? <div className="h-24 animate-pulse rounded-xl bg-[#edf3fc]" /> : exports.data?.length ? <div className="divide-y divide-[#e7eef8] overflow-hidden rounded-xl border border-[#dce7f5] bg-white/85">{exports.data.map(item => <article key={item.id} className="flex flex-wrap items-center justify-between gap-3 p-3"><div className="min-w-0"><div className="flex flex-wrap items-center gap-2"><strong className="text-sm text-[#486482]">Sharing audit CSV #{item.id}</strong><Badge className={item.status === "ready" ? "bg-[#e8f4e9] text-[#3d7652]" : item.status === "failed" ? "bg-[#fff0eb] text-[#a4513c]" : "bg-[#fff4db] text-[#95671e]"}>{item.status}</Badge></div><p className="mt-1 text-[11px] text-[#75899f]">Generated {new Date(item.createdAt).toLocaleString()} · {item.requestedBy.name || item.requestedBy.email || "Administrator"}</p><p className="mt-1 text-[11px] text-[#8093a8]">Filter: {item.eventFilter} · {item.startAt ? new Date(item.startAt).toLocaleDateString() : "all time"} to {item.endAt ? new Date(item.endAt).toLocaleDateString() : "today"}</p></div><div className="flex flex-wrap items-center gap-2">{item.url ? <Button asChild size="sm" variant="outline" className="border-[#cbdcf1] text-[#4e6f9d]"><a href={item.url}><Download className="mr-1 size-3.5" />Download</a></Button> : null}<Button size="sm" variant="outline" disabled={setArchive.isPending} className="border-[#d7e1f1] text-[#587595]" onClick={() => setArchive.mutate({ exportId: item.id, archived: !archived })}>{setArchive.isPending ? <Loader2 className="mr-1 size-3.5 animate-spin" /> : archived ? <ArchiveRestore className="mr-1 size-3.5" /> : <Archive className="mr-1 size-3.5" />}{archived ? "Restore" : "Archive"}</Button></div></article>)}</div> : <div className="rounded-xl border border-dashed border-[#dbe5f3] bg-white/70 p-7 text-center"><FileSpreadsheet className="mx-auto size-5 text-[#7898c1]" /><strong className="mt-2 block text-sm text-[#4d6e9b]">No {archived ? "archived" : "completed"} sharing-audit CSVs</strong><p className="mt-1 text-xs leading-5 text-[#788ba2]">Use the CSV button in the audit dashboard to create a managed export for this history.</p></div>}</CardContent></Card>;
}
