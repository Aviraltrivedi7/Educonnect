import { Badge } from "@/components/ui/badge";
import { Button } from "@/components/ui/button";
import { Card, CardContent, CardDescription, CardHeader, CardTitle } from "@/components/ui/card";
import { trpc } from "@/lib/trpc";
import { ArrowRight, BookOpenCheck, ClipboardCheck, MessageSquareText, ShieldAlert, Sparkles, UserRoundCheck } from "lucide-react";

type DashboardPage = "courses" | "assignments" | "assessments" | "messages";

function FocusAction({ label, onClick }: { label: string; onClick: () => void }) {
  return <button type="button" onClick={onClick} className="group inline-flex items-center gap-1 text-xs font-semibold text-[#376e50] transition-colors hover:text-[#1f4f37] focus-visible:outline-none focus-visible:ring-2 focus-visible:ring-[#82a589] focus-visible:ring-offset-2"><span>{label}</span><ArrowRight className="size-3 transition-transform duration-150 group-hover:translate-x-0.5 motion-reduce:transition-none" /></button>;
}

export function StudentLearningFocus({ onNavigate }: { onNavigate: (page: DashboardPage) => void }) {
  const focus = trpc.workspace.studentLearningFocus.useQuery();
  if (focus.isLoading) return <Card className="border-[#e0e6dd]"><CardContent className="h-48 animate-pulse bg-[#f6faf4]" /></Card>;
  const data = focus.data;
  return <Card className="overflow-hidden border-[#d9e6d8] bg-[linear-gradient(135deg,#f7fbf5_0%,#ffffff_66%)]"><CardHeader className="border-b border-[#e6eee3] pb-4"><div className="flex items-start justify-between gap-3"><div><div className="mb-2 inline-flex items-center gap-2 rounded-full bg-[#e2f1df] px-2.5 py-1 text-[10px] font-bold uppercase tracking-[.12em] text-[#397252]"><Sparkles className="size-3" />Personal focus</div><CardTitle className="text-lg">Your next best learning steps</CardTitle><CardDescription>Live priorities built from your enrolled courses, work, and teacher feedback.</CardDescription></div><span className="grid size-10 place-items-center rounded-2xl bg-[#dfeede] text-[#317357]"><BookOpenCheck className="size-5" /></span></div></CardHeader><CardContent className="space-y-3 pt-4"><div className="grid gap-3 md:grid-cols-3"><section className="rounded-xl border border-[#dcead9] bg-white p-3"><div className="flex items-center justify-between"><span className="text-[10px] font-bold uppercase tracking-[.11em] text-[#5d7665]">Continue learning</span><BookOpenCheck className="size-4 text-[#4d9670]" /></div>{data?.nextLesson ? <><strong className="mt-3 block line-clamp-2 text-sm text-[#2e4537]">{data.nextLesson.title}</strong><p className="mt-1 text-xs text-[#718077]">{data.nextLesson.course?.courseCode} · {data.nextLesson.durationMinutes ? `${data.nextLesson.durationMinutes} min` : "Self-paced"}</p><div className="mt-3"><FocusAction label="Resume lesson" onClick={() => onNavigate("courses")} /></div></> : <><strong className="mt-3 block text-sm text-[#2e4537]">You are caught up</strong><p className="mt-1 text-xs leading-5 text-[#718077]">No unfinished published lessons in your active courses.</p></>}</section><section className="rounded-xl border border-[#f0e3d9] bg-white p-3"><div className="flex items-center justify-between"><span className="text-[10px] font-bold uppercase tracking-[.11em] text-[#8b654f]">Upcoming work</span><ClipboardCheck className="size-4 text-[#d57855]" /></div>{data?.upcomingAssignment ? <><strong className="mt-3 block line-clamp-2 text-sm text-[#573f31]">{data.upcomingAssignment.title}</strong><p className="mt-1 text-xs text-[#8a756a]">{data.upcomingAssignment.courseCode} · {data.upcomingAssignment.dueAt ? `Due ${new Date(data.upcomingAssignment.dueAt).toLocaleDateString()}` : "No deadline"}</p><div className="mt-3"><FocusAction label="Open assignment" onClick={() => onNavigate("assignments")} /></div></> : <><strong className="mt-3 block text-sm text-[#573f31]">No open assignments</strong><p className="mt-1 text-xs leading-5 text-[#718077]">New published work will show up here when it needs your attention.</p></>}</section><section className="rounded-xl border border-[#e8e0f2] bg-white p-3"><div className="flex items-center justify-between"><span className="text-[10px] font-bold uppercase tracking-[.11em] text-[#705b88]">Latest feedback</span><MessageSquareText className="size-4 text-[#8866b4]" /></div>{data?.recentFeedback[0] ? <><strong className="mt-3 block truncate text-sm text-[#4d3d64]">{data.recentFeedback[0].title}</strong><p className="mt-1 line-clamp-2 text-xs leading-5 text-[#756886]">“{data.recentFeedback[0].feedback}”</p><div className="mt-3"><FocusAction label="Review assessments" onClick={() => onNavigate("assessments")} /></div></> : <><strong className="mt-3 block text-sm text-[#4d3d64]">Feedback will appear here</strong><p className="mt-1 text-xs leading-5 text-[#718077]">Comments on your graded assessments and assignments stay easy to revisit.</p></>}</section></div><div className="flex flex-wrap items-center justify-between gap-3 rounded-xl border border-[#dbe9d9] bg-white/75 px-3 py-2.5"><span className="text-xs text-[#587063]"><strong className="text-[#2e4937]">{data?.activeCourseCount ?? 0}</strong> active courses are informing this focus view.</span><div className="flex flex-wrap gap-2"><Button size="sm" variant="outline" className="h-8 border-[#c8dcc8] bg-white text-[#38664a]" onClick={() => onNavigate("courses")}>My courses</Button><Button size="sm" className="h-8 bg-[#2f7452] text-white hover:bg-[#276345]" onClick={() => onNavigate("assessments")}>Practice assessments</Button></div></div></CardContent></Card>;
}

export function TeacherLearnerAttention({ onNavigate }: { onNavigate: (page: DashboardPage) => void }) {
  const attention = trpc.workspace.teacherLearnerAttention.useQuery();
  if (attention.isLoading) return <Card className="border-[#e0e6dd]"><CardContent className="h-48 animate-pulse bg-[#fffaf7]" /></Card>;
  const data = attention.data;
  return <Card className="overflow-hidden border-[#f0ded1] bg-[linear-gradient(135deg,#fffaf7_0%,#ffffff_70%)]"><CardHeader className="border-b border-[#f2e6dc] pb-4"><div className="flex items-start justify-between gap-3"><div><div className="mb-2 inline-flex items-center gap-2 rounded-full bg-[#fde9df] px-2.5 py-1 text-[10px] font-bold uppercase tracking-[.12em] text-[#a15539]"><ShieldAlert className="size-3" />Teaching pulse</div><CardTitle className="text-lg">Learner attention queue</CardTitle><CardDescription>Priorities are calculated from your course ownership, submissions, and lesson progress.</CardDescription></div><span className="grid size-10 place-items-center rounded-2xl bg-[#fde6dc] text-[#c66042]"><UserRoundCheck className="size-5" /></span></div></CardHeader><CardContent className="grid gap-4 pt-4 lg:grid-cols-[.9fr_1.1fr]"><section className="rounded-xl border border-[#f1e3da] bg-white p-3"><div className="flex items-center justify-between"><strong className="text-sm text-[#5e4436]">Ready for grading</strong><Badge className="bg-[#fff0e9] text-[#b65a3b]">{data?.pendingCount ?? 0}</Badge></div>{data?.pendingSubmissions.length ? <div className="mt-3 space-y-2">{data.pendingSubmissions.map(submission => <div key={submission.id} className="rounded-lg bg-[#fffaf7] p-2.5"><strong className="block truncate text-xs text-[#654a3b]">{submission.assignmentTitle}</strong><span className="mt-1 block truncate text-[11px] text-[#897569]">{submission.studentName || submission.studentEmail || "Learner"} · {submission.courseCode}</span></div>)}</div> : <p className="mt-3 text-xs leading-5 text-[#718077]">No submitted work is waiting in courses you manage.</p>}<div className="mt-3"><FocusAction label="Open grading workflow" onClick={() => onNavigate("assignments")} /></div></section><section className="rounded-xl border border-[#f1e3da] bg-white p-3"><div className="flex items-center justify-between"><strong className="text-sm text-[#5e4436]">Progress follow-up</strong><Badge className="bg-[#fff5e8] text-[#a66c24]">{data?.learnersNeedingFollowUp.length ?? 0}</Badge></div>{data?.learnersNeedingFollowUp.length ? <div className="mt-3 space-y-2">{data.learnersNeedingFollowUp.map(learner => <div key={`${learner.studentId}-${learner.courseId}`} className="flex items-center justify-between gap-3 rounded-lg bg-[#fffaf7] p-2.5"><div className="min-w-0"><strong className="block truncate text-xs text-[#654a3b]">{learner.studentName || learner.studentEmail || "Learner"}</strong><span className="mt-1 block truncate text-[11px] text-[#897569]">{learner.courseCode} · {learner.completedLessons}/{learner.totalLessons} lessons</span></div><span className="rounded-full bg-[#fff0df] px-2 py-1 text-[10px] font-bold text-[#a46622]">{learner.completionPercent}%</span></div>)}</div> : <p className="mt-3 text-xs leading-5 text-[#718077]">Every enrolled learner with published lessons is at or above the current follow-up threshold.</p>}<div className="mt-3"><FocusAction label="Open messages" onClick={() => onNavigate("messages")} /></div></section><div className="lg:col-span-2 flex flex-wrap items-center justify-between gap-3 rounded-xl border border-[#f1e3da] bg-white/75 px-3 py-2.5"><span className="text-xs text-[#7d685b]">Use the queue to grade submitted work and follow up before learners fall further behind.</span><div className="flex gap-2"><Button size="sm" variant="outline" className="h-8 border-[#eccfc0] bg-white text-[#98533a]" onClick={() => onNavigate("messages")}>Message learners</Button><Button size="sm" className="h-8 bg-[#c96546] text-white hover:bg-[#ae5035]" onClick={() => onNavigate("assignments")}>Review submissions</Button></div></div></CardContent></Card>;
}
